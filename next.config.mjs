/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'images.unsplash.com',
      'media.dodostatic.net',
      'cdn.dodostatic.net',
    ],
  },
}

export default nextConfig
